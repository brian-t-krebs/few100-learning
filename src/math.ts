

//private to the source code file by default  If you want this to be available in other files, you need to use the export before the function.

export function add(a: number, b: number) { return a + b };

export function subtract(a: number, b: number) {
    return a - b;
}

